import 'package:flutter/material.dart';

class AppColors {
  // Brand - Corporate Premium (Light Mode / Orange)
  static const Color primary = Color(0xFFFF9F43); // Vibrant Orange
  static const Color primaryDark = Color(0xFFE88A30);
  static const Color accent = Color(0xFFFFC07D); // Lighter Orange
  
  // Backgrounds - Off-white / pale beige
  static const Color background = Color(0xFFFFF5EC);
  static const Color surface = Color(0xFFFFFFFF); // White cards
  static const Color surfaceElevated = Color(0xFFFFFFFF); // White cards
  
  // Text - Dark on Light
  static const Color textPrimary = Color(0xFF1E293B); // Slate 800 (Dark Gray)
  static const Color textSecondary = Color(0xFF64748B); // Slate 500 (Gray)
  static const Color textTertiary = Color(0xFF94A3B8); // Slate 400 (Light Gray)
  
  // Status
  static const Color success = Color(0xFF10B981); // Emerald 500
  static const Color warning = Color(0xFFF59E0B); // Amber 500
  static const Color error = Color(0xFFEF4444); // Red 500
  static const Color info = Color(0xFF3B82F6); // Blue 500
  
  // Utility
  static const Color transparent = Colors.transparent;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color divider = Color(0xFFE2E8F0); // Slate 200
}
